- vars:
    hosts: coreos
    become: no
  hosts: "{{hosts}}"
  gather_facts: no

  roles:
    - raw_python
    - hostname
    - coreos_config

    - locksmithd
    - docker

    - dotfiles
    - pipework
