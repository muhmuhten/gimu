- hosts: fedora
  tasks:
    - action: "{{ansible_pkg_mgr}} name=iptables-services"
    - command: >
        systemctl enable iptables.service
        creates=/etc/systemd/system/basic.target.wants/iptables.service
